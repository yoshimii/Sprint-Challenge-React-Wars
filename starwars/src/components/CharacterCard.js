import React from 'react';

const CharacterCard = props => {
    
    return (

        <div>
            <div>{props.name} </div>
            <div> </div>
            <div> </div>
        </div>

    );
};

export default CharacterCard;